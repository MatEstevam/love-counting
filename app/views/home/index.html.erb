<div class="min-h-screen bg-gradient-to-br from-pink-200 via-red-200 to-yellow-200 flex flex-col items-center justify-center p-6 text-center font-sans">

  <!-- Título -->
  <h1 class="text-5xl font-extrabold mb-8 text-red-700 drop-shadow-lg">
    Desde Que Você Chegou...
  </h1>

  <!-- Carrossel -->
  <div data-controller="carousel" class="relative w-full max-w-xl overflow-hidden">
    <div data-carousel-target="container" class="flex transition-transform duration-500 ease-in-out">

      <!-- Clonado: última imagem -->
      <%= image_tag "foto5.JPEG", alt: "Clone last", class: "w-full object-cover flex-shrink-0" %>

      <!-- Slides reais -->
      <%= image_tag "foto4.JPEG", alt: "Foto 4", class: "w-full object-cover flex-shrink-0" %>
      <%= image_tag "foto6.JPEG", alt: "Foto 6", class: "w-full object-cover flex-shrink-0" %>
      <%= image_tag "foto1.JPEG", alt: "Foto 1", class: "w-full object-cover flex-shrink-0" %>
      <%= image_tag "foto2.JPEG", alt: "Foto 2", class: "w-full object-cover flex-shrink-0" %>
      <%= image_tag "foto3.JPEG", alt: "Foto 3", class: "w-full object-cover flex-shrink-0" %>
      <%= image_tag "foto5.JPEG", alt: "Foto 5", class: "w-full object-cover flex-shrink-0" %>

      <!-- Clonado: primeira imagem -->
      <%= image_tag "foto4.JPEG", alt: "Clone first", class: "w-full object-cover flex-shrink-0" %>
    </div>

    <!-- Indicadores -->
    <div class="absolute z-30 flex -translate-x-1/2 bottom-5 left-1/2 space-x-3">
      <% 6.times do |i| %>
        <button type="button"
                class="w-3 h-3 rounded-full bg-white/50"
                aria-label="Slide <%= i + 1 %>"
                data-action="click->carousel#showSlide"
                data-index="<%= i %>">
        </button>
      <% end %>
    </div>
  </div>


 <!-- Nomes -->
  <p class="text-2xl font-semibold my-2 text-red-800">Isabelli & Matheus</p>

  <!-- Data do início -->
  <p class="text-lg text-red-600 mb-4">Desde 24 de agosto de 2024</p>

  <!-- Contador -->
  <p
    data-controller="contador"
    data-contador-target="contador"
    data-contador-data-inicial-value="2024-08-24T00:00:00"
    class="text-xl font-medium text-red-900 mb-4">
  </p>



  <!-- Mensagem poética -->
  <blockquote class="italic text-red-700 max-w-xl mx-auto mb-4 px-4">
    “Entre todas as voltas que o mundo dá, a melhor foi quando ele me trouxe até você.
    Nosso amor não tem ponto final — só vírgulas para continuarmos escrevendo juntos.”
  </blockquote>

  <!-- Música Spotify Embed -->
  <div class="w-full max-w-xs mx-auto mb-4 p-4">
    <iframe
      src="https://open.spotify.com/embed/track/1x3W8RZxW94lrVGhP95qA6"
      width="100%"
      height="80"
      frameborder="0"
      allowtransparency="true"
      allow="encrypted-media"
      loading="lazy"
      ></iframe>

  <!-- Botão whatsapp -->
  <div class="fixed bottom-4 right-4 z-50">
    <a href="https://w.app/matheusestevam" target="_blank">
      <i class="fab fa-whatsapp text-green-500 text-4xl"></i>
    </a>
  </div>

</div>
